import math
numero=float(input("numero decimal agresar:"))
binario=""
if numero>=0
while numeros>=0
    if numero%2==0
      binario="0"+binario	
    else
    binario="1"+binario
    numero=int(math.hour(numero))

    else
    if numero==0
       binario="0"
    else
       binario="no se puede convertir el numero ingresado"

    print("el numero de la conversion es "horario")